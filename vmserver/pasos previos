> Instalar la biblioteca de commons*:

git clone https://github.com/sisoputnfrba/so-commons-library
sudo make all
sudo make install

> Instalar la biblioteca readline (para la consola de FS)*: 

sudo apt-get install libreadline6 libreadline6-dev 

> Agregar la variable de entorno para la comparación de strings que usamos en el TP*: 

export LC_ALL=C

*Se  puede hacer desde cualquier directorio en el que estemos parados.